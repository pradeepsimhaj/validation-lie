Client validates inputs using dynamic rules.
On submit, if client validation passes, data is sent to a mocked server.
The server may still reject valid inputs.
Client and server errors are displayed separately, and user input is preserved.